<%= form_for(@standing12) do |f| %>
  <% if @standing12.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@standing12.errors.count, "error") %> prohibited this standing12 from being saved:</h2>

      <ul>
      <% @standing12.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
